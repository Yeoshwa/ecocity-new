// JS pour les inputs EcoCity
// (Ajoutez ici des comportements custom si besoin)
// Exemple : focus automatique sur le premier input
window.addEventListener('DOMContentLoaded', () => {
  const firstInput = document.querySelector('.eco-input');
  if(firstInput) firstInput.focus();
});
