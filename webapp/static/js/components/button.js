// JS pour les boutons EcoCity
// (Ajoutez ici des comportements custom si besoin)
document.querySelectorAll('.eco-btn').forEach(btn => {
  btn.addEventListener('click', e => {
    // TODO: comportement custom global bouton
  });
});
